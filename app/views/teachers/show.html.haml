.teacherbackground
  .row
    .col-md-6.col-lg-12.teacher
      %h2.teacher
        = @teacher.name
        %span.text-muted 
          = @teacher.grade
  .row 
    .col-md-2.col-md-offset-1.teachlist 
      .list-group-item 
        .text-center       

          = link_to edit_teacher_path(@teacher) do
            %span.glyphicon.glyphicon-pencil
  
          = link_to teacher_path(@teacher), method: :delete do
            %span.glyphicon.glyphicon-remove

    
  .row
    .col-md-6.col-md-offset-3
      .list-group-item
        .text-center
          %p.lead.teaching 
            I teach the following reading groups:
            %br
          - @programs.each do |program|
            %h3.lead
              = program.name 
            Focus:
            = program.focus
          %br
/ 
            
      